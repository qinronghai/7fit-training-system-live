# 7Fit Training System V14.6.3｜十大动作模式整合版 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 将用户可见的“八大动作模式 + SUPPORT + CORE”统一升级为“十大动作模式”，同时保留底层 `eightPatterns`、SUP-S1–S6、CORE-L1–L4/Core Demand 的既有算法和数据结构。

**Architecture:** 新增 `V14_DATA.tenPatternCatalog` 作为顶层信息架构目录；01–08 引用现有 T1–T4 主链，09 跳转现有 SUPPORT，10 跳转现有 CORE。F111、PREP、Foam、Anatomy、Conflict 继续使用原底层数据，不让目录层反向污染训练算法。

**Tech Stack:** Static HTML/CSS, vanilla JavaScript, Python/pytest, Node.js runtime tests.

**Spec:** `/mnt/data/7Fit_Training_System_V14.6.3_十大动作模式整合版设计规范.md`

## Global Constraints
- 用户可见主名称统一为“十大动作模式”。
- 01–08 保持 T1–T4；09 保持 S1–S6；10 保持 L1–L4 + Core Demand。
- 保留 `eightPatterns` 内部变量，不做无意义重命名。
- 不改变 F111 32 套课程、PREP、Foam、239/239 Anatomy、Conflict、Prescription/RIR、Venue Route。
- 十大模式总览必须可“复制本模块”。
- SUPPORT/CORE 原详情页继续可复制。

---

### Task 1｜隔离 V14.6.3 并建立失败测试
- [ ] 从 V14.6.2 ZIP 解压到 `/mnt/data/7fit-training-system-v14_6_3`。
- [ ] 跑 V14.6.2 基线测试。
- [ ] 新增测试：`tenPatternCatalog` 必须存在且 10/10。
- [ ] 新增测试：01–08 type=`main_pattern`，09=`support`，10=`core`。
- [ ] 新增结构测试：用户可见页面出现“十大动作模式”，不存在旧主标题“八大动作模式”。

### Task 2｜新增 `tenPatternCatalog`
- [ ] 在 `data/system-data.js` 增加 10 项 catalog。
- [ ] 01–08 仅引用现有主模式 key，不复制第二套 T 链。
- [ ] 09 指向 SUPPORT，10 指向 CORE。
- [ ] 数据测试通过。

### Task 3｜训练体系页面整合
- [ ] 将训练体系首页改成 10 张模式卡 + PREP 热身入口。
- [ ] 01–08 进入现有主模式详情。
- [ ] 09 进入现有 SUPPORT 详情，显示 S1→S6。
- [ ] 10 进入现有 CORE 详情，显示 L1→L4 + Core Demand。
- [ ] 移除顶层重复的 SUPPORT/CORE 独立入口，但保留路由兼容。

### Task 4｜复制系统升级
- [ ] 新增“十大动作模式”总览复制 formatter。
- [ ] 输出 01–10，并分别显示 T1–T4 / S1–S6 / L1–L4 + Core Demand。
- [ ] 单模式复制继续调用现有 T/S/CORE 模块 formatter。
- [ ] 测试复制内容不把 SUPPORT/CORE 错写成 T1–T4。

### Task 5｜维护页、文案与回归
- [ ] Maintenance 增加 `Ten Pattern Catalog 10/10`。
- [ ] 用户可见“八大动作模式 / 八大模式 / Eight Patterns”改为“十大动作模式 / Ten Movement Patterns”。
- [ ] 技术审计可保留 `eightPatterns 8/8`。
- [ ] 跑全 Python tests、所有 Node runtime tests、全部 JS syntax。
- [ ] 生成单文件 preview、ZIP、V14.6.3 validation report。

## Final Acceptance
```text
Ten Pattern Catalog     10/10
Main T Patterns           8/8
SUPPORT                   30/30
CORE                      20/20
F111                      32/32
Runtime Anatomy          239/239
PREP                      20/20
Foam                      12/12
Copy System               PASS
Conflict Logic            PASS
Python tests              PASS
JS syntax/runtime         PASS
Missing assets             0
```
