# 7Fit Training System V14.5｜验证报告

验证对象：V14.5 动作肌群解剖层  
基线：V14.4 泡沫轴肌群匹配版

## 1｜Phase A Anatomy Coverage

| 项目 | 结果 |
|---|---:|
| Phase A 唯一 anatomy 节点 | 120 / 120 |
| F111 / 八大模式 / D1-D2 力量动作 | 40 / 40 |
| SUPPORT canonical | 30 / 30 |
| CORE canonical | 20 / 20 |
| PREP canonical | 20 / 20 |
| Foam Roll canonical | 12 / 12 |
| Runtime actions | 239 |
| Phase A 缺失 | 0 |

说明：SUPPORT / CORE / PREP 存在复用 actionId，因此各体系数量相加不等于 120 个唯一节点。

## 2｜Anatomy 可信度

| Confidence | 数量 |
|---|---:|
| HIGH | 102 |
| MEDIUM | 16 |
| REVIEW | 2 |

REVIEW 节点：
- `warmup_band_shoulder_horizontal_circle`｜弹力带肩关节水平绕环（左右）——具体轨迹等待馆内示范视频锁定。
- `warmup_band_front_raise_external_rotation`｜弹力带前平举位肩外旋——具体手臂高度 / 轨迹等待馆内示范视频锁定。

`confidence=review` 可正常显示，但不进入精准 `MUSCLE_CONCENTRATION` 分数计算。

## 3｜V14Anatomy Runtime

已验证：
- `get(actionId)`
- `labels(actionId)`
- `aggregate(actionIds)`
- `aggregateSession(sessionId, selectedIds)`
- `compareToBaseline(sessionId, selectedIds)`
- `rankFoam(actionIds)`
- `rankWarmups(actionIds)`
- anatomy 缺失 graceful degradation
- REVIEW 节点不进入精准暴露分数

结果：PASS。

## 4｜课程 / PREP / Foam 行为

- F111 课程可显示「本节主要训练肌群」。
- 当前替换动作变化后，肌群摘要随整页重渲染重新计算。
- PREP 优先按实际动作肌群 + 关节 + 模式 + L/T 等级排序；anatomy 不足时回退 V14.4 模式匹配器。
- Foam Roll 优先读取 A/B/D1/D2 的主要 / 辅助肌群，最多返回 4 个高相关区域；anatomy 不足时回退 V14.4 模式匹配器。
- `MUSCLE_CONCENTRATION` 仅为 soft WARN，不产生 FAIL。
- 触发阈值：current >= 3.0 且 delta vs baseline >= 1.0。

结果：PASS。

## 5｜动作详情 / 系统维护

- Phase A 动作详情显示「肌群解剖」。
- Coach Mode 显示角色对应的主要 / 辅助 / 稳定 / 关节信息。
- System Mode 额外显示 Confidence 与 Source。
- 非 Phase A 动作 anatomy 缺失时显示「解剖数据待补齐」，不阻断动作详情或替换。
- 系统维护显示 Anatomy Coverage、HIGH / MEDIUM / REVIEW、Phase A 缺失和 Phase B/C 未覆盖运行节点。

结果：PASS。

## 6｜自动化验证

| 验证项 | 结果 |
|---|---|
| Python pytest | 33 passed |
| JS syntax (`data/*.js`, `js/*.js`) | PASS |
| Anatomy runtime tests | PASS |
| MUSCLE_CONCENTRATION test | PASS |
| PREP anatomy ranking | PASS |
| Foam anatomy ranking | PASS |
| Relative asset check | missing assets = 0 |

## 7｜Chromium Browser Smoke

由于当前执行环境的组织策略会直接拦截 `localhost` 与 `file://` 导航，本次通过 Chromium DevTools Protocol 的 `Page.setDocumentContent` 将完全内联的 V14.5 页面载入真实 Chromium 144 渲染进程，再进行 DOM 与交互验证；这避免把环境 URL 策略误判为页面故障。

已验证：
1. `#/coach/f111-01/l1` 等价 hash 路由渲染 6 个正式 session slots。
2. 页面显示「本节主要训练肌群 / 主要刺激 / 协同参与 / 核心 / 稳定」。
3. 替换动作后整页重新计算，6 个槽位仍完整。
4. 当前课程 Foam / PREP 推荐仍存在并重新计算或安全回退。
5. Phase A `hake_shendun` 动作详情显示「肌群解剖 / 主要肌群 / 股四头肌」。
6. System-mode 动作详情显示 `Confidence=high` 与 Source。
7. Maintenance 显示 Anatomy Coverage 120 / 120。
8. 390 px viewport：document scrollWidth = 390，body scrollWidth = 390，无横向溢出。
9. Uncaught JavaScript exceptions = 0。

结果：PASS。

## 8｜Excel Reconciliation

输出：`7Fit-器械清单与动作库-2026-09-08_V14.5动作肌群解剖.xlsx`

- 新增独立工作表：`动作肌群解剖`
- Website Phase A records = 120
- Excel anatomy rows = 120
- Missing from Excel = 0
- Duplicate Phase A IDs = 0
- Spreadsheet formula error scan = 0
- REVIEW 记录使用「V14.5 待动作视频复核」，其余使用「V14.5 已审查」。

结果：120 / 120 PASS。

## 9｜非回归约束

保持不变：
- F111 32 套课程
- 八大模式 V1.1 8 / 8
- SUPPORT 30 / 30
- CORE 20 / 20
- PREP 20 / 20
- Foam Roll 12 / 12
- 6 个正式 F111 槽位结构
- Venue Route
- Source Tier / Standard Tier 分层
- 原有 conflict FAIL / WARN / INFO 逻辑
- POST CARDIO 不进入正式 strength slots

V14.5 anatomy 是独立增量层，不成为既有编课功能的强依赖。
